const param = require('./param');
const welcome = require('./welcome');
const priceMsg = require('./priceMsg');
const collective = require('./collective');

module.exports = {
    param,
    welcome,
    priceMsg,
    collective,
};
